import 'package:flutter/material.dart';

class InviteFriendPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Invite a Friend"),
      ),
      body: Center(
        child: Text("Invite a Friend Page"),
      ),
    );
  }
}
